; ==================================================
; AD Tun Interface Configuration
; Author: Arkaprava Das
; ==================================================

[ad_tun]

; Name of the TUN interface to create
ifname = ad_tun0

; IPv4 address of the TUN interface
ipv4 = 10.10.0.2/24

; IPv6 address of the TUN interface (optional)
ipv6 = fd00:1234:5678::2/64

; Maximum Transmission Unit
mtu = 1500

; Persist interface after process exits (0 = no, 1 = yes)
persist = 1


; ================================
; AD-Transport Server Configuration
; ================================

[ad_transport]

; Server address for traffic forwarding (format: IP:PORT)
server_addr = 203.0.113.10:5000
